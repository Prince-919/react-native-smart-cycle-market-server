{
  "name": "server",
  "version": "1.0.0",
  "description": "This is the api build for smart-cycle-market",
  "main": "index.js",
  "scripts": {
    "dev": "tsnd respawn -r tsconfig-paths/register --transpile-only ./src/server.ts",
    "build": "tsc && tsc-alias",
    "start": "node ./dist"
  },
  "author": "Prince Sharma",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "ts-node-dev": "^2.0.0",
    "tsc-alias": "^1.8.10",
    "tsconfig-paths": "^4.2.0"
  }
}
